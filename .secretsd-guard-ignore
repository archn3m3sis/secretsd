# Paths whose credential-shaped content is deliberate.
# One glob per line. Keep this list short and specific — every entry is a place
# the guard stops looking.
test/*
bin/lib/guard.sh
.github/workflows/*
README.md
